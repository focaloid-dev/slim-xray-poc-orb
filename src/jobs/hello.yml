description: >
  Sample description
# What will this job do?

executor: default

parameters:
  to:
    type: string
    default: "Slim"
    description: "This is a slim description"
steps:
  - greet:
      to: << parameters.to >>
