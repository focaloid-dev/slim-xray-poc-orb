description: >
  Sample description
# What will this job do?

executor: default

parameters:
  imageName:
    type: string
    default: "Slim testing hello"
    description: "This is a slim description"
steps:
  - scan:
      imageName: << parameters.imageName >>
